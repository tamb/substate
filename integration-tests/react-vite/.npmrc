# React Integration Test - Dependency Isolation
# Ensures this test project's dependencies stay completely isolated

# npm configuration for dependency isolation
legacy-peer-deps=true
package-lock-only=false
prefer-dedupe=false
