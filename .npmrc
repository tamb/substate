# Substate - Complete Dependency Isolation Configuration
# This prevents ANY hoisting of dependencies to ensure integration tests
# are completely isolated and realistic

# Disable all forms of hoisting
hoist=false
shamefully-hoist=false
public-hoist-pattern=""

# Additional safety measures
auto-install-peers=false
prefer-workspace-packages=false
